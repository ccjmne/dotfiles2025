#! /bin/sh

readonly lock=$XDG_RUNTIME_DIR/${0##*/}.lock
exec 9<> $lock
if flock --nonblock 9; then
	trap 'ydotool click 0x80' EXIT
	ydotool click 0xc0 --repeat=99999999 &
	readonly pid=$!
	echo $pid 1>&9
	wait $pid
else
	kill $(cat $lock)
fi
