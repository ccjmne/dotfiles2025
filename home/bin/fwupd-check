#! /bin/sh

fwupdmgr get-updates
rc=$?

if [ $rc -eq 0 ]; then
    notify-send 'Firmware update check' 'Some updates are available' \
        --action='Run: fwupdmgr update'                              \
        | ifne ghostty -e ack fwupdmgr update
elif [ $rc -eq 2 ]; then
    notify-send 'Firmware update check' 'No updates available'
else
    exit $rc
fi
