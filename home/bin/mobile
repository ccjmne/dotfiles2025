#! /bin/sh -e

readonly mobile=192.168.0.185
avahi-browse --parsable --terminate --resolve _adb-tls-connect._tcp \
    | grep -o "$mobile;[[:digit:]]*" | head -1 | tr \; :            \
    | xargs adb connect
scrcpy --turn-screen-off --max-size=1080
