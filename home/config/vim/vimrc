syntax          on
filetype indent on
filetype plugin on

packadd comment
packadd hlyank

se nu rnu

let v:clipproviders['wl'] = {
    \  'copy':  {'+': {_, __, lines -> system('wl-copy', lines)}},
    \  'paste': {'+': {_ -> ['', systemlist('wl-paste')]}},
    \}
set clipmethod^=wl
